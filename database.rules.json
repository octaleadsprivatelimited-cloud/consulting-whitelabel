{
  "rules": {
    ".read": true,
    ".write": "auth != null && (auth.token.email == 'admin.procyonsolutions@gmail.com' || auth.token.email == 'findlocalsg@gmail.com' || auth.token.email.endsWith('@procyonsol.com'))"
  }
}
